{
    "name": "gitbook",
    "scripts": {
        "build": "gitbook install && gitbook build --log=debug --debug",
        "deploy": "npm run build && push-dir --branch=gh-pages --dir=_book --cleanup",
        "development": "gitbook serve"
    },
    "dependencies": {
        "push-dir": "^0.4.1"
    },
    "devDependencies": {}
}
